import { TeamClient } from "./team-client"

export const dynamic = "force-dynamic"
export const metadata = { title: "Team - SANTADDEO" }

export default function TeamPage() {
  return <TeamClient />
}
